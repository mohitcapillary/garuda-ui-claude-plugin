# {{APP_NAME}} — Architecture Overview

> **Generated**: {{TIMESTAMP}}
> **Scanned from**: {{REPO_ROOT}}

---

## Executive Summary

<!-- 3-minute read for a new engineer.
     Cover: what the app does, who uses it, core tech stack,
     top 3 architectural decisions, and how the codebase is organized. -->

{{EXECUTIVE_SUMMARY}}

---

## High-Level Architecture

<!-- Describe the overall architecture pattern (Atomic Design + Redux-Saga),
     how the app bootstraps (Vulcan SDK), and the major layers:
     Pages → Organisms/Molecules/Atoms → Services → Redux Store → Sagas → API.
     Include a simple ASCII diagram if helpful. -->

{{HIGH_LEVEL_ARCHITECTURE}}

---

## Feature Modules

<!-- Group by BUSINESS DOMAIN, not by directory path.
     For each domain, list:
     - Module name and purpose (1 sentence)
     - Key components (pages, organisms)
     - Redux artifacts: reducer key, saga effects, main actions
     - Notable patterns or deviations from conventions

     Keep to module-level summary. Do NOT list every file. -->

{{FEATURE_MODULES}}

---

## Data Flow: Redux + Redux-Saga

<!-- Explain the standard data flow pattern used across the app:
     1. UI Event (user click, form submit, page mount)
     2. Action dispatch (from component or connected container)
     3. Saga interception (watcher saga → worker saga)
     4. API call (via services/api.js and requestConstructor.js)
     5. Success/failure action dispatch from saga
     6. Reducer update (pure state transformation)
     7. Selector recomputation (reselect memoization)
     8. Component re-render via react-redux connect/useSelector

     Include: dynamic reducer/saga injection pattern,
     how feature pages register their own store slices. -->

{{DATA_FLOW}}

---

## Shared Technical Layers

<!-- Document cross-cutting layers used by multiple feature modules:
     - Services (api.js, requestConstructor.js, localStorageApi.js)
     - Utilities (hooks, HOCs, domain helpers)
     - Config (endpoints, paths, constants)
     - Auth (authWrapper, redux-auth-wrapper)
     - i18n (react-intl, Locize)
     - Error handling (error boundaries)
     - Analytics/monitoring (GTM, NewRelic)
     
     For each: what it provides, where it lives, how features consume it. -->

{{SHARED_LAYERS}}

---

## Integration Points

<!-- Identify every boundary where this frontend talks to something external:
     - Backend APIs (via endpoints.js — summarize API domains, not every endpoint)
     - Platform SDK (@capillarytech/vulcan-react-sdk — what it provides)
     - Capillary libraries (cap-ui-library, cap-ui-utils, cap-audience-manager, etc.)
     - Third-party services (NewRelic, Locize, etc.)
     
     For each: what crosses the boundary, which direction, and what contract governs it. -->

{{INTEGRATION_POINTS}}

---

## Key Packages & Libraries

<!-- List ONLY packages that materially shape the architecture.
     For each: name, version, and 1-sentence role description.
     Omit: dev-only tools, transpilers, loaders, test utilities.
     Target: 10-15 packages maximum. -->

| Package | Version | Architectural Role |
|---------|---------|-------------------|
{{KEY_PACKAGES_TABLE}}

---

## Architectural Conventions

<!-- Document the rules and patterns the codebase follows:
     - Atomic Design hierarchy (atoms → molecules → organisms → pages)
     - Cap-* component usage requirement
     - CapRow/CapColumn layout convention
     - Dynamic reducer/saga injection pattern
     - File organization within feature modules
     - Naming conventions (actions, constants, selectors)
     - Styling approach (styled-components)
     - Code quality rules (500-line limit, complexity cap)
     - PropTypes requirement -->

{{CONVENTIONS}}

---

## Assumptions, Constraints & Risks

<!-- Call out:
     - Assumptions: what must remain true for this architecture to hold
     - Constraints: hard limits (Node version, React Router version, etc.)
     - Known risks: architectural debt, scalability concerns, fragile areas
     - Known limitations: what this architecture cannot easily support -->

### Assumptions
{{ASSUMPTIONS}}

### Constraints
{{CONSTRAINTS}}

### Risks & Limitations
{{RISKS}}

---

## Metadata

- **Files scanned**: {{FILE_COUNT}}
- **Feature modules identified**: {{MODULE_COUNT}}
- **Components**: {{ATOM_COUNT}} atoms, {{MOLECULE_COUNT}} molecules, {{ORGANISM_COUNT}} organisms
- **Redux artifacts**: {{REDUCER_COUNT}} reducers, {{SAGA_COUNT}} sagas
- **Generated by**: architect-scan agent
- **Timestamp**: {{TIMESTAMP}}
